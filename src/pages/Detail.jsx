// detail page

import React from 'react'

export default function Detail() {
  return (
    <div>detail</div>
    // 데이터 불러오기 전에 Skeleton UI 적용
    
    // 페이지 제목: 검색결과 (도시 이름) 동적 렌더링

    // 현재 날씨 정보: 날씨 상태(맑음, 흐림 등), 현재 온도, 체감 온도, 습도, 풍속
    // 날씨 아이콘이 현재 날씨에 따라 변경됨 
    // (OpenWeather API 제공 아이콘, 혹은 react-icons 라이브러리)

    // 페이지 하단 → 5일 예보: 버튼 클릭시 아코디언처럼 펼쳐지는 구조
  )
}
